# M8.9 "The look" — design and implementation plan

Executor: the agent that starts on Marcin's word. Reviewer: Claude, at the
gate. Director and the only tester: Marcin. This document is the milestone's
design and its contract in one place, as M8.8's: the game's look taken from
Marcin's six out of ten to nine or ten. The world at a real golden hour and
lit at dusk, one colour for one meaning, one typeface in two styles, a HUD
that scales with the screen, one message at a time, a radar that answers
three questions, signs that read from afar and never block the view, and the
garage as a showroom with a picture on every card. Written 2026-09-25
against commit `52db2bc` (0.8.7) from a review of every screen (§1.1) that
Marcin accepted whole, with his bar: "6/10: it must be 10/10, at least 9".
About nineteen days of slices.

**Where it sits.** After M8.8's gate and before M9, by the number Marcin
gave it (2026-09-25). The look dresses what the fleet adds: every new body
has its picture in the showroom, the sea and the dirt take the new light, and
the platform's thumbnail, cover and screenshots (M9) are taken of this look.
**Slice 0 does not wait**: it fixes two faults in the build Marcin plays and
goes in on his word before M8.8's slice 0, or first thing if M8.8 has begun.

Read, in this order, before touching anything: `CLAUDE.md`, §1 of this file,
`docs/DESIGN.md` §17 (the clean screen), §19 (Polish) and §20 (the way),
`docs/STYLE.md` (UI, the run HUD, the job signs), `docs/M8.8_REPORT.md` (the
bodies, the ground and the sea it added), `docs/PROGRESS.md` (newest first),
then the rest of this file. Run `npm run verify`; green before the first
edit.

## 0. How to work on this milestone

- **Language, autonomy, scope, honesty**: as in `docs/M5_PLAN.md` §0.
- **Pace** (`CLAUDE.md`): a slice is the code, its pins, the quick verify,
  one commit, eight lines in PROGRESS. **One exception, because this
  milestone's subject is how the game looks**: a slice that changes what the
  screen or the world looks like ends with its stills: `npx playwright test
  e2e/look.spec.ts -g stills` (slice 1 adds it), the slice's own states at
  1280×720, under a minute, looked at before the commit; the numbers it
  prints go into the slice's PROGRESS lines. No other suite, no perf, no bot
  measurement per slice. Marcin can take the exception back with a word.
- **Marcin's notes come first**, each a slice ahead of the queue.
- **Every slice ends in a build he can play**; the old look is not kept
  behind a switch (the before set of slice 1 is the comparison).
- **Plain words in every message to him**: the stars, the money, the map,
  the garage; never token, zoom, glow attribute or atlas.
- **Every new word has its Polish in the same slice** (`src/ui/pl.ts`,
  `tests/ui/lang.test.ts`) and its place in `tests/ui/words.test.ts`.
- **The phases run in order** (A to G), the slices inside a phase too.

## 1. The design

### 1.1 What there is (measured 2026-09-25)

Every state was captured in Polish at 800×450, 1280×720 and 1920×1080: the
intro step by step, every job kind's sign, card and line, a chase at three
and at five stars, busted, the door's four pages and its offer, the full map,
the pause, the four districts (about 170 frames; the review's sheet of twelve
is in PROGRESS). What holds: the calm drive shows §17's six; the type is one
consistent arcade voice (heavy italic capitals, flat black shadows); the
route on the radar and the signs' pictograms work; nothing is ugly. What
does not, measured:

1. **The world is grey, not candy.** On the four districts' calm frames the
   mean saturation is 0.18–0.21 and only 1.7–5.5 % of the pixels are over
   0.5, most of them the player's car. The sun is 1.8 at 51° over the
   horizon (`SUN_OFFSET`), a midday angle; the hemisphere's fill is 1.35,
   three quarters of the sun, and the shadows are at 0.72: sunlit and shaded
   faces differ little and nothing looks like the last hour of the evening.
   The facades' colours (`CITY_COLORS`) sit at saturation 0.1–0.35; no
   window or lamp is lit; the sky is two colours and has no sun.
2. **The HUD's words sit on the sky and only their shadow carries them.**
   Against the sky at the top of the frame ink reaches 1.8–2.3:1, yellow
   1.4–1.8, red 1.4–1.7 and cyan 1.1–1.4 (large text wants 3:1, small text
   4.5:1); on the asphalt 3.3–10:1. LOSE THEM (ZGUB ICH) in red on the pink
   sky is the weakest line of the chase.
3. **A colour means many things.** Yellow is money, and also the selected
   tab and button, the titles (PAUZA), the highway and the player's arrow
   and the caches on the radar, the combo's ×, a full boost bar and the
   wall's sentence. Cyan is the way, and also NEW BEST, the next car's line,
   the streak, the hunts, the best run, the escape bar, the video icon, the
   combo at ×5, a kept car's frame and the next rival's chip. Red is trouble
   and the lead of a teaching line (ZLECENIE · ZWOLNIJ W KÓŁKU).
4. **The type is the system's and the sizes are fixed.** Segoe UI on
   Windows, another face elsewhere. Heavy italic plus a −8° to −14° skew
   slants the letters about 20°; with a hard shadow at 12–14 px (the cards'
   lines, the hints, the labels) the Polish marks read badly. Words are
   11–20 px whatever the screen (NITRO 11, the hints 12, the district 12,
   the line 20); only the speed and the stars follow the height a little. At
   1920×1080 the goal line is 20 px; at 800×450 the radar (a 150 px floor)
   is a third of the height and the eight key hints wrap to five rows.
5. **The moments are crowded.** The intro's first second shows nine things
   and the stars' news. The busted moment in a session's first 12 s shows
   the line, two rows of hints, the news, the stars, the bag, BORROW with its
   disguise clock, the ticket and the officer: two opposite orders. The
   first job brings its card, then the chain's step card saying NEXT: BANK
   THE BAG while the delivery runs, then NEW: STREET RACE, then NEW: TIME
   TRIAL, about nine seconds of reading over a timed job.
6. **The radar carries sixteen kinds of mark in 165 px**: the route, the
   goal's badge, open and closed badges (14.5 px; their pictograms read as
   white dots), three garages and four landmarks on the rim (the tower is a
   cream triangle beside the player's yellow one), up to thirty caches as
   yellow dots, the units lit and on the beat, the search, a race's rivals,
   the player, N. The brightest lines are the white streets and the yellow
   highway, not the route. The full map's key names all seven job kinds
   before they are out, and the districts' names lie over its icons.
7. **The world's guidance.** A sign reads to about 50 m and is gone by 90;
   the ring on the asphalt is not seen; seven coins round every ring pull a
   player through it fast, and the ring says slow down. Two faults of M8.7:
   the pay over the nearest sign never leaves the screen's top-left corner
   (`PayLabel.show` compares the new point with `NaN` and never writes it),
   and driving through a ring's centre the camera flies through its sign,
   whose face fills the screen.
8. **What a child cannot read.** The biggest number on screen, the bag (44
   px yellow), has no glyph and no word; §17.4's sack was never drawn. Two ×
   show at once (the combo's ×1 top left, the bag's ×1,65 top right). The
   stars have three states (yellow, red and pulsing, pulsing over a cyan
   bar) and a red +n under them. The loop (crimes fill the bag, the stars
   multiply it, a garage banks it) is said only on the wall, after the first
   door. Three pictograms need their word: the police light (escape), the
   crash star (takedown rage), the hammer (mayhem).
9. **The garage is a text sheet over the car it dresses.** The cars, the
   kit and the rivals are colour swatches with names; the wall covers the
   car; the room is a grey box with a black ceiling seen from its back
   corner.
10. **Faults that are not faults.** At five stars the helicopter's cone and
    spot wash the road and the sky in a flat pale shape; a thinned car's
    4×4 screen door reads as a coarse halftone at the frame's bottom; the
    pause's veil lies under the goal line and the money.
11. **Words.** KÓŁKO and KRĄG for a ring; R is RESET in the hints and OD NOWA
    on WRAK; the busted card says W DOWOLNY KLAWISZ; two lines are in lower
    case; OSŁONA is an abstract word for a roofed street.

### 1.2 The bar: nine out of ten

Marcin's mark decides. The gate shows him a board: twelve states before and
after at 800×450, 1280×720 and 1920×1080 (the intro's first second, a calm
drive, a sign at 50 m, a job's card, the first job's step, a chase at three
stars, five stars with the helicopter, the busted moment, TOTALS, CARS,
STYLE, the full map) and the four districts' calm drives. What can be
measured is measured, in the look suite or a pin:

| What | Target | Today |
|---|---|---|
| The world's mean saturation (the canvas alone, the four districts' calm frames) | ≥ 0.30 | 0.18–0.21 |
| Pixels saturated over 0.5 | ≥ 10 % | 1.7–5.5 % |
| The facades' band (rows 30–55 % of the height, outside the car) | ≥ 0.25 | not measured |
| A sunlit chalk facade's brightest channel | < 250 | — |
| The sky behind the HUD (the frame's top 15 %, about 13°–22° up), luminance | ≤ 0.18 | about 0.36 |
| Any text at 800×450 | ≥ 13 px | 11 px |
| Each colour token against its outline | ≥ 4.5:1 | no outline |
| The top centre in any state | the line and one more | up to four |
| The radar's kinds of mark while driving | R6's list | sixteen |
| The goal's badge and the route on the radar at 800×450 | ≥ 17 px, ≥ 5 px | 14.5, 5 |
| A sign on screen, to 150 m, at 720p | ≥ 28 px | 0 at 90 m |
| The garage | the car in view on every page, a picture on every card | neither |
| Every number on screen | with its glyph or word | the bag has none |
| The fonts' bytes; the draws; the frame p95 | ≤ 80 KB; +2; not above M8.8's | — |

### 1.3 The rules (accepted by Marcin; the numbers set here)

**R1. One colour, one meaning.** On the screen, the maps and the world's
marks (the signs, the rings, the route):

| Meaning | Token | Colour | Where |
|---|---|---|---|
| money: the bag, a pay, a price, a coin, a cache | `--money` | `#ffd23f` | the bag and its ×, the pays, the prices, the money pops, the caches |
| the way: where to go and what to press next | `--way` | `#2bd1ff` | the route, the goal's ring, sign and badge, the swap prompt |
| trouble: bad for you | `--trouble` | `#ff3b5c` | the stars while seen (with blue), WPADKA, WRAK, a clock under 10 s, a price out of reach, LOSE THEM, a race's rivals on the maps |
| the police | `--police` | `#3b82ff` | the units, the search, the helicopter, the stars' flash |
| everything else | `--ink` | `#f7f3ea` | words, the player on the maps, selection (a white frame or fill), the combo, titles that are not money |
| closed, off | `--off` | `#8d8a96` | closed rings, owned and not chosen, units on the beat, dimmed buttons |
| outline and panel | `--outline`, `--panel` | `#160e28`, 78 % | every outline, every panel |

The old uses move by the table: selection and titles to ink, the highway to
a wide pale road, the player's arrow to ink, the combo to ink, a full boost
to ink with one pulse, the wall's highlights (NEW BEST, the next car, the
streak, the hunts, the best run) to ink with their amounts in yellow, the
escape bar to ink, the video icon to an ink frame, a kept car's and the next
rival's frames to white, a teaching line's lead to ink. **The stars** are
ink when filled and flash red and blue at 2 Hz, like a light bar, while the
police see the player (steady red under `prefers-reduced-motion`); the
search's bar is ink. The world's own colours (paint, facades, billboards,
the sea) are not signals and stay out of the table.

**R2. One typeface, two styles.** Rubik (SIL Open Font License), in the
build as woff2 subsets of the game's characters: Bold 700, Black 900, Black
Italic 900, about 60 KB, preloaded, `font-display: swap`. STYLE's "no web
fonts" goes: its reasons (bytes, offline, licence) are met by 60 KB of OFL
files inside the build.

- **Display** (numbers, titles, the line's kind, the pays, a card's title;
  20 px and up): Black Italic, capitals, a 2 px outline and a 2×3 px flat
  shadow. **No text is skewed**: the italic carries the motion; panels and
  bars keep their −8°.
- **Label** (everything 18 px and under: hints, a card's line, the wall's
  lines, legends, names): Bold, upright, capitals, tracking 0.06 em, a
  1.5 px outline.
- Every text over the world has its outline. Numbers in tabular figures.
  The ★ in a text is the HUD's star drawn inline, not a font's glyph.

**R3. The HUD scales with the screen.** Its sizes are in rem, and the root's
font size is 16 px × the scale: the window's height over 720, held between
0.85 and 1.5 (450 px tall: 0.85; 720: 1; 1080: 1.5). A label is at least
1 rem (13.6 px at 800×450); the display sizes are today's. The radar, the
gauge and the wall take the same scale; the wall's own steps (×1.2, ×1.4)
go.

**R4. The corners, finished.** Each still answers §17.2's one question.

- **The stars** (top right): five, outlined, no panel; the next star fills
  from its foot as the heat rises (no +n); the flash of R1.
- **The money block** under them: the bank first (the coin glyph, 22 px
  display, ink), the bag under it (the sack glyph, 44 px display, yellow,
  its × from ×1.3); the pops' lane under the block. The bank is first, so it
  never jumps and no gap is kept for the bag.
- **The combo** (top left): KOMBO, its points, its × from ×2 in ink, the
  last trick's word, its bar in ink.
- **The gauge** (bottom right) mirrors the radar: one round dial, 150 px at
  720p, on a dark disc like the radar's: the speed in its middle, km/h under it,
  the boost as its outer arc (270°, ink, a flame glyph at its start; full, it
  pulses once; boosting, it brightens), the damage as an inner arc for 3 s
  after a hit and standing from the third stage, in red. The words NITRO and
  USZKODZENIA go: the intro's SHIFT NITRO teaches the arc.
- The sack, the coin and the flame are outlines in `src/sim/glyphs.ts`, one
  drawing wherever they appear.

**R5. One moment, one message.**

- The top centre holds the goal line and at most one more: a caption, a
  card or a message. The rest waits, in this order: the intro's caption,
  the running job's card and its result, a warning (ROADBLOCK AHEAD,
  HELICOPTER), the stars' news, a teaching line, the chain's step card, a
  NEW card. News that has waited 4 s is dropped; cards wait.
- While a job's clock runs, the chain's step card and the NEW cards wait
  for its result.
- The busted moment: nothing speaks but the ticket. A police car alongside
  puts its prompt on the ticket as one block (E POŻYCZ); the disguise's
  clock shows once the car is borrowed.
- The key hints are four (drive, boost, map, pause) in one row for 8 s, in
  the first two sessions, only when the top is otherwise empty; the full
  list is on the pause screen.
- Inside the intro the captions speak alone; the stars' news and the
  district's name wait for a quiet moment. The damage shows on a hit, not
  standing (R4).
- **Teaching at the moment**, once per profile each, saved: the first bag
  (ŁUP · WPŁAĆ GO W GARAŻU), the first × (GWIAZDKI MNOŻĄ ŁUP), the first
  chase (ZGUB ICH · ZNIKNIJ IM Z OCZU).
- **A card** is a title, one line of at most 34 characters in either
  language, and its number; it stays 2.5 s at least. The line's clock and
  distance say the rest.

**R6. The radar answers three questions**: where to go, where the police
are, where to bank.

- It draws: the streets mid-grey on the dark ground, the highway and the
  loop as wider pale roads (no yellow); the district tints and the water;
  the route in cyan, 6 px on a dark 2 px edge each side, the brightest line
  of the disc; the goal's badge, 20 px (ink face, cyan ring, pictogram);
  open rings as 6 px ink dots and closed ones grey; the units blue (flashing
  with the stars while they chase), those on the beat grey, the search
  disc; a race's rivals as small red arrows; the nearest garage's house on
  the rim, only while the bag holds money or the line says BANK IT or BUY;
  a cache as a gold dot only within 80 m; the player as a 14 px ink arrow
  with a dark edge; N.
- The landmarks, the other garages, the caches beyond 80 m, the cameras,
  the cover and the breakers are on the full map only.
- 165 px at 720p, in the scale.
- **The full map** uses the same colours and marks, badges of 12 px with
  their pictograms, the route at 6 px, the player's ink arrow; its key lists
  only the kinds on the map now; each district's name sits at a point clear
  of every icon.

**R7. Signs that read and never block.**

- A sign is never smaller on screen than 28 px at 720p (in the scale):
  beyond the distance where its 1.4 m face would be, it is drawn larger
  about its centre. The goal's sign is 1.25× the others.
- A sign taller on screen than 15 % of the height folds away over 0.15 s
  and comes back when it is smaller or behind the camera: the camera never
  enters a sign.
- The label over the nearest open sign ahead within 100 m names its kind
  and its pay (UCIECZKA +3000), placed over the sign.
- No coins round the rings (§3.2's ring of seven); a running job's route
  coins stay.

**R8. The golden hour.** Starting values, tuned on the stills against §1.2:

- The sun about 25° over the horizon (51° today), keeping its bearing; warm
  gold (`#ffc27a`), about 2.4. The fill about half of today's (0.7): a cool
  violet sky (`#8f8fd6`) over a warm ground bounce (`#9a7560`). Shadows at
  0.9. Sunlit faces glow warm, shaded faces go violet.
- The sky in three stops, as at dusk: hot peach at the horizon (`#ffb487`),
  rose about 5° up (`#d9849a`), deep violet from about 15° up (`#3d3a7a`),
  so the frame's top 15 % (about 13°–22° up), where the HUD sits, is dark
  enough for ink at 4.5:1 without its outline. The fog and the background
  are the horizon's stop.
- `CITY_COLORS` a quarter more saturated, the trims and chalk kept light;
  each district's accent a clean, strong colour on its canopies, rails and
  fascias (Crown gold, the Works teal, the Gardens' flower boxes pink, the
  Quay aqua and coral); the asphalt a notch bluer; the markings brighter;
  the grass, the dirt and the sea under the same light.

**R9. The city lit at dusk, with depth.** A glow per vertex in the city's
shared material, added after the light and before the fog, no new draw and
no light: a quarter of the upper windows warm-lit (chosen by the lot, the
floor and the bay, the same every time), six in ten shop windows, the lamp
heads. Every facade darker at its foot (0.8 at the ground to 1.0 at 12 m)
with a contact band in its lowest 0.4 m (0.7). **The sky** gets the sun's
disc and halo where the light comes from, and a dozen flat clouds lit orange
from below in the sun's half of the sky, 8°–25° up; both ride with the
camera as the dome does, one draw each.

**R10. The garage is a showroom.**

- The door shut, the camera leaves the back corner in 0.6 s for the
  showroom's view: the car in the room's middle, three-quarters from the
  front, turning on a turntable at 8°/s (drawn only; the car in the game
  does not move), framed in the left part of the screen; the wall slides in
  on the right, 45 % of the width at most (424–576 CSS px at the ten sizes).
- The room: a lit ceiling (strip lights, no black void), a painted bay on
  the floor, the posters' wall lit, the tool wall, the house glyph in neon.
  One warm light for the room, there from the start at zero and raised at
  the door.
- **Every card has its picture**: a car (the catalogue, kept, found, won,
  M8.8's) in its paint, three-quarters from the front; a topper, wheels, a
  spoiler, a stance on a grey car; neon, a flame, smoke as a swatch in the
  effect's shape; a horn as a speaker; a rival as its car. Rendered at the
  session's first door, a few a frame while the wall is up, kept for the
  session; never during a drive. A card is its picture, its name, its role
  or BEST AT line (M8.8) and its price.
- **The focus previews**: paint, wheels, a spoiler, the stance, a topper and
  neon go on the car while focused, a flame and smoke show in a 1 s puff, a
  horn sounds once; the turntable turns the car to show it (the roof, the
  tail); leaving restores what it wears; buying keeps it.
- TOTALS shows the car too. GOALS in pictures: the day's three with a bar
  each, the board as the rivals' cars (the beaten ticked, the next framed),
  the hunts as three counters with their glyphs, the best run.

**R11. One word per thing, in capitals.** KÓŁKO is a job's ring, STREFA a
rage or mayhem zone (ROZBIJAJ AUTA W STREFIE, DEMOLUJ WSZYSTKO W STREFIE;
the duel's READY line: GOTOWE · KÓŁKO JEST NA MAPIE); R is OD NOWA (RESET in
English) in the hints and on WRAK; OSŁONA becomes UKRYCIE; the busted card
says NACIŚNIJ DOWOLNY KLAWISZ with no keycap; every line is in capitals
(NACIŚNIJ P, ABY GRAĆ DALEJ; E WEŹ AUTO · R OD NOWA; the landmarks' names
too). KRĄG, OSŁONA and RESET never appear in Polish.

**R12. Nothing looks like a fault.**

- The helicopter's light is a spot on the ground with a soft edge and a
  lamp on its belly; no cone.
- A thinned car's steady state is a one-pixel checker (it reads as half
  transparent); the 4×4 pattern only while it fades.
- The pause's veil covers the whole screen.
- **The pause**: PAUZA in ink, the resume line, the sound, the settings,
  the full list of keys, the build small.
- **The loading screen**: the sky's three stops behind it, the new type and
  a thin bar over the boot's phases.
- **WPADKA and WRAK**: the new type and colours; WRAK sits above the
  debris' flight.

### 1.4 What stays out, and why

- **The name, the logo, the tab's title and the cover** (Marcin: when the
  time comes). M9's cover brief takes this milestone's frames.
- **Textures, normal maps, a colour grade or a bloom pass.** The brief's
  flat-shaded, textureless look stays, and the low tier gets no new
  full-screen pass (the speed lines' stays the only one). The look comes
  from the light, the palette and the vertex colours.
- **Real lights in the city and a day–night cycle.** Lit windows and lamps
  are colour; the time of day is fixed (STYLE).
- **New systems, jobs, cars, HUD elements or balance numbers.** The rings'
  coins are the one change, quoted at the gate.
- **A second typeface** and any font service: the files are in the build.
- **Touch** (M9). M9 lays its buttons over the corners and moves the radar
  as its plan says; the scale and the gauge serve it.
- **Music and sound**, but for the horn heard on its card.
- **Rebuilding the city's geometry**: only the colours and the attributes
  of the meshes that are there.

## 2. Scope

### 2.1 In scope (§3, slices 0–16)

- **A. The faults and the ruler**: the pay label and the sign in the
  camera; the look suite and the before set (0–1).
- **B. The world**: the golden hour; the city lit, with depth; the sun and
  the clouds (2–4).
- **C. The base**: one colour, one meaning; one typeface in two styles; the
  scale (5–7).
- **D. The screen**: the corners and the gauge; one moment, one message;
  the radar and the full map; the words, the pause, the loading, the cards
  (8–11).
- **E. The world's marks**: the signs; the helicopter's light; the thinned
  car; the rings' coins (12).
- **F. The garage**: the showroom; the pictures; the preview and GOALS in
  pictures (13–15).
- **G. The gate** (16).

### 2.2 Out of scope

§1.4; new jobs, cars or systems; any new npm dependency (the fonts are
assets, and the subsetter runs once on a developer's machine);
`docs/BRIEF.md`; loosening any pin or budget.

### 2.3 Fixed by the brief and still binding

Low-poly and flat-shaded, no per-asset textures, the palette in
`src/sim/palette.ts`; the CrazyGames legibility at the ten sizes at DPR 1;
the ads through the adapter, the offer's two buttons the same size, a
rewarded button's video icon; control never taken for over 2 s (the
showroom's camera moves behind a shut door, a break the game makes);
keyboard first. Everything else in `docs/M5_PLAN.md` §1.4.

## 3. Slices

### Phase A — the faults and the ruler

#### Slice 0 — two faults and the pause's veil (0.5 day, before M8.8 on Marcin's word)

The pay over the nearest open sign is placed at its first show: `PayLabel`
started at `NaN`, `Math.abs(x - NaN) > 0.5` is false, and the label has sat
in the screen's top-left corner since M8.7's slice 3. It names the kind with
the pay (R7). A sign taller on screen than 15 % of the height folds away over
0.15 s and comes back when it is smaller or behind the camera. The pause's
veil takes the top layer, over the line, the bag, the bank, the wall and the
full map. Reason: two faults in the build Marcin plays; M8.7's report lists
the label as working.

Files: `src/ui/hud/pay.ts`, `src/app/App.ts`, `src/render/run/MarkerView.ts`,
`src/render/run/signs.ts`, `src/ui/hud/hud.ts`, `src/ui/styles.css`,
`src/ui/pl.ts`; tests `tests/ui/pay.test.ts` (new),
`tests/render/signs.test.ts`.

Pins: M8.9 0.1 the label's first show writes its place (a pure `moved()`
that is true from `NaN`); 0.2 its words for every kind in both languages;
0.3 `signFold(heightPx, screenPx)` folds above 15 % and not below; 0.4 the
pause's layer is the top one (the layers' order as a constant).

#### Slice 1 — the look suite and the before set (0.5 day)

`e2e/look.spec.ts` takes §1.2's twelve states and the four districts' calm
drives by the review's recipes (`?manual=1`, the QA hooks, `?lang=pl`). At
1280×720 with `-g stills`, a slice's own states; at the gate, all of them at
800×450, 1280×720 and 1920×1080. It reads the world's numbers from the WebGL
canvas alone (drawn into a 2D canvas in the frame's own task, so the HUD is
not in them): the mean saturation, the share over 0.5, the facades' band,
the top 15 %'s luminance, a sunlit chalk facade's brightest channel. It reads
the HUD's from the DOM: every visible text's size, what shows at the top
centre. It prints them, and asserts them only with `LOOK_GATE=1`. Run once on
this tree: the before set in `screens/look/before/` (git-ignored, kept for
the gate's board) and its numbers in PROGRESS.

Files: `e2e/look.spec.ts`, `package.json` (`npm run look`), `docs/DEV.md`.

Pins: none (an e2e suite; it runs at the gate and as the stills).

### Phase B — the world

#### Slice 2 — the golden hour (1 day)

R8: the sun lowered to about 25° on its bearing, warmer and stronger; the
fill halved, a cool sky over a warm ground; shadows at 0.9; the sky's three
stops; the fog and the background on the horizon's stop; `CITY_COLORS` a
quarter more saturated, the districts' accents, the asphalt, the markings,
the grass, the dirt (M8.8) and the sea. The stills before and after, and
their numbers, in PROGRESS.

Files: `src/render/sky.ts` (a `SKY` table), `src/render/shadows.ts`
(`SUN_OFFSET`), `src/sim/palette.ts`, `src/render/city/CityView.ts` (the
sea, the ground); tests `tests/render/sky.test.ts` (new),
`tests/render/shadows.test.ts`.

Pins: 2.1 the sun's elevation between 20° and 30°; 2.2 the sky's colour 20°
up has a luminance of 0.18 or less; 2.3 the fog and the background equal the
horizon's stop; 2.4 the shadow texel grid's pins hold on the new direction.

#### Slice 3 — the city lit, with depth (1.5 days)

R9's glow: a normalized byte per vertex in the city's chunks and the lamp
props, added in the shared material after the light and before the fog, and
chained after the road paint and the shadow's edge fade. A quarter of the
upper windows lit, six in ten shop windows, the lamp heads; the facades'
gradient and contact band written into their vertex colours at the build.

Files: `src/render/city/glow.ts` (new: `litWindow`, `GLOW`),
`src/render/city/CityView.ts`, `src/render/city/roadPaint.ts` (the chain),
`src/render/props/propMesh.ts`, `src/render/props/PropsView.ts`; tests
`tests/render/glow.test.ts` (new), `tests/render/idleDraws.test.ts`.

Pins: 3.1 the lit share of a district's upper windows is 22–28 %; 3.2 the
same city lights the same windows; 3.3 the gradient's factor is 0.8 at the
foot and 1.0 from 12 m; 3.4 the draw counts unchanged.

#### Slice 4 — the sun and the clouds (0.5 day)

R9's sky: the sun's disc and halo (an additive fan in vertex colours) where
the light comes from, twelve flat clouds lit orange from below in the sun's
half of the sky, both riding with the camera.

Files: `src/render/sky.ts`, `src/render/city/skyline.ts`; tests
`tests/render/sky.test.ts`.

Pins: 4.1 the disc's direction equals the light's; 4.2 the clouds between 8°
and 25° up, in the sun's half; 4.3 two draws added in all.

### Phase C — the base

#### Slice 5 — one colour, one meaning (1 day)

R1's tokens in `:root` and in `src/ui/colors.ts` (for the maps, the signs
and the world's marks); every use moved by R1's table; the stars' flash; the
search's bar in ink; the +n under the stars goes (the meter comes in slice
8).

Files: `src/ui/styles.css`, `src/ui/colors.ts` (new),
`src/ui/map/minimap.ts`, `src/ui/map/bigmap.ts`, `src/render/run/signs.ts`,
`src/ui/hud/heat.ts`, `src/ui/hud/jobs.ts`, `src/ui/hud/run.ts`,
`src/ui/hud/voice.ts`, `src/ui/hud/coldOpen.ts`, `src/ui/wall/garage.ts`;
tests `tests/ui/colors.test.ts` (new).

Pins: 5.1 no colour literal in `styles.css` outside `:root` (the dev panel
and the fake ad excepted by their selectors); 5.2 the maps' and the signs'
colours come from `colors.ts`; 5.3 `colors.ts` equals the CSS tokens; 5.4
each token against `--outline` at 4.5:1 or more; 5.5 `--way` is used only by
the selectors R1 names.

#### Slice 6 — one typeface, two styles (1.5 days)

R2. The subsets made once with fonttools' `pyftsubset` from the Rubik
repository's static TTFs (the download is asked for), for Basic Latin,
Latin-1, the Polish letters, the dashes, the quotes and the ellipsis; the
files and their ranges committed; `@font-face` with `unicode-range`, the
display weight preloaded in `index.html` by a relative path. The two styles
as the only text rules; the skew off every text; the outline on every text
over the world; the ★ drawn inline; tabular figures; the maps' words in
Rubik once `document.fonts.load` has it. The licence in `docs/ASSETS.md`.

Files: `src/ui/styles.css`, `index.html`, `src/ui/fonts.ts` (new: the
ranges, the load), `src/ui/hud/stars.ts` (new: the star's drawing, inline),
`src/ui/hud/voice.ts`, `src/ui/hud/jobs.ts`, `src/ui/map/minimap.ts`,
`src/ui/map/bigmap.ts`, `docs/ASSETS.md`, the font files; tests
`tests/ui/fonts.test.ts` (new).

Pins: 6.1 every character the screen can say (`pl.ts`'s values, the English
keys, the numbers' separators) is in the subsets' ranges or is ★; 6.2 no text
rule carries a skew (the selectors that may: the panels and bars); 6.3 the
fonts' files 80 KB or less.

#### Slice 7 — the scale (1 day)

R3: the HUD's sizes in rem (a one-off rewrite of `styles.css`'s pixel sizes
outside the dev panel), the root's font size from `hudScale(height)` on every
resize, the radar's and the gauge's sizes in rem, every label at least 1 rem.
The wall and the full map take the scale in slices 13 and 10, where their
layouts change.

Files: `src/ui/styles.css`, `src/ui/scale.ts` (new: `HUD_SCALE`,
`hudScale`), `src/app/App.ts`, `src/ui/map/minimap.ts` (its backing store
from its rect × DPR); tests `tests/ui/scale.test.ts` (new).

Pins: 7.1 `hudScale` at 450, 720 and 1080 px is 0.85, 1 and 1.5; 7.2 no
`font-size` in pixels in the HUD's rules, and none under 1 rem; 7.3 the
radar's size in rem.

### Phase D — the screen

#### Slice 8 — the corners and the gauge (1.5 days)

R4: the stars without their panel, the next star's fill (`heatFill`); the
money block (the bank, then the bag with the sack); the combo named KOMBO
with its × from ×2; the gauge with the speed, the boost's arc and the
damage's; the sack, the coin and the flame in `sim/glyphs.ts`; the bag's ×
hidden at ×1 on the wall's sums too.

Files: `src/ui/hud/heat.ts`, `src/ui/hud/corners.ts`, `src/ui/hud/hud.ts`,
`src/ui/hud/run.ts`, `src/ui/hud/totals.ts`, `src/ui/styles.css`,
`src/sim/glyphs.ts`, `src/sim/heat/Heat.ts` (the levels' thresholds read);
tests `tests/ui/corners.test.ts`, `tests/ui/heat.test.ts` (new).

Pins: 8.1 the calm drive shows its six (the line, the stars, the bank, the
radar, the gauge's speed and boost); 8.2 `heatFill` is 0 at a level's start
and 1 at the next, rising in between; 8.3 the combo's × hidden at ×1; 8.4 the
damage's arc 3 s after a hit, standing from the third stage, never at the
first two without a hit; 8.5 the bag's × hidden at ×1, on the HUD and on the
wall.

#### Slice 9 — one moment, one message (1.5 days)

R5: the top voice's queue; nothing from the chain during a timed job; the
busted block; the four hints in the first two sessions and the full list on
the pause screen; the intro's captions alone; the three teaching lines; the
cards' one line.

Files: `src/ui/hud/voice.ts` (the queue), `src/ui/hud/lanes.ts`,
`src/ui/hud/jobs.ts`, `src/ui/hud/hud.ts`, `src/ui/hud/coldOpen.ts`,
`src/ui/hud/corners.ts` (`hintRows`), `src/ui/settings.ts` (the pause's
list), `src/sim/save/format.ts` (the taught flags and the sessions, the next
version), `src/ui/pl.ts`; tests `tests/ui/voice.test.ts`,
`tests/ui/lanes.test.ts`, `tests/sim/save.test.ts`,
`tests/ui/words.test.ts`, `tests/ui/lang.test.ts`.

Pins: 9.1 never two besides the line; the order; news dropped after 4 s;
9.2 during a timed job no step or NEW card, and after its result they come in
order; 9.3 while the ticket fills only the ticket (and its prompt) speaks;
9.4 four hints, in sessions one and two only; 9.5 each teaching line once per
profile across a save's round trip; 9.6 every card's line at most 34
characters in both languages.

#### Slice 10 — the radar and the full map (1.5 days)

R6.

Files: `src/ui/map/minimap.ts`, `src/ui/map/minimapModel.ts`,
`src/ui/map/bigmap.ts`, `src/ui/styles.css`; tests
`tests/ui/minimap.test.ts`.

Pins: 10.1 the radar draws only R6's kinds (the model reports what it drew,
per state); 10.2 the route is the brightest line of the disc; 10.3 the goal's
badge 20 px and the route 6 px at 720p, at least 17 and 5 at 800×450; 10.4 a
garage on the rim only with a bag or a BANK IT or BUY goal; 10.5 the full
map's key lists only the kinds on the map; 10.6 no district name's box over
an icon's.

#### Slice 11 — the words, the pause, the loading, the cards (0.5 day)

R11 and R12's pause, loading, WPADKA and WRAK.

Files: `src/ui/pl.ts`, `src/ui/lang.ts`, `src/ui/hud/hud.ts`,
`src/ui/hud/run.ts`, `index.html`, `src/ui/styles.css`; tests
`tests/ui/lang.test.ts`, `tests/ui/words.test.ts`.

Pins: 11.1 KRĄG, OSŁONA and RESET never in Polish; KÓŁKO and STREFA each in
its place; 11.2 the HUD's root sets capitals, and no table value the screen
shows is in lower case; 11.3 the pause's list names every key.

### Phase E — the world's marks

#### Slice 12 — signs that read; faults that are not faults (1 day)

R7's minimum size and the goal's 1.25×; the rings' coins removed; R12's
helicopter light and thinned car.

Files: `src/render/run/MarkerView.ts`, `src/render/run/signs.ts`,
`src/sim/jobs/place.ts`, `src/render/run/Coins.ts`,
`src/render/police/HeliView.ts`, `src/render/cars/bodyMesh.ts`,
`src/render/camera/fade.ts`; tests `tests/render/signs.test.ts`,
`tests/render/fade.test.ts`, the jobs' pins that count the rings' coins.

Pins: 12.1 `signScale(distance, fov, height)` keeps 28 px at 720p to 150 m
and is 1 where the face is larger; 12.2 the goal's 1.25; 12.3 no coin within
6 m of a marker; 12.4 the checker at the steady fade.

### Phase F — the garage

#### Slice 13 — the showroom (2 days)

R10's camera, turntable, room and light, and the wall on the right, its
pages laid out for 424–576 CSS px: TOTALS fits 800×450 without scrolling;
CARS, STYLE and GOALS scroll; the keys' grid as it is.

Files: `src/render/camera/CameraDirector.ts`,
`src/render/camera/ChaseCamera.ts`, `src/render/run/HideoutView.ts`,
`src/render/cars/PlayerCar.ts`, `src/render/Renderer.ts` (the room's light),
`src/ui/wall/garage.ts`, `src/ui/wall/wallPages.ts`,
`src/ui/wall/wallGrid.ts`, `src/ui/hud/run.ts`, `src/ui/styles.css`; tests
`tests/render/hideout.test.ts`, `tests/render/camera.test.ts`,
`tests/ui/wall.test.ts`, `tests/ui/wallGrid.test.ts`.

Pins: 13.1 the showroom's camera sees the car's whole box in the left 55 %
at the ten sizes (a projection); 13.2 the light count is the same from boot
to the door and back; 13.3 the turntable is drawn only (the car's pose in
the game unchanged across a door); 13.4 the wall's keys walk the same grid.

#### Slice 14 — the pictures (1.5 days)

R10's pictures: an atlas of 160×100 cells rendered at the session's first
door, a few cells a frame while the wall is up, kept; every card's picture
from it.

Files: `src/render/cars/thumbs.ts` (new), `src/render/Renderer.ts`,
`src/ui/wall/garage.ts`, `src/ui/styles.css`; tests
`tests/render/thumbs.test.ts` (new).

Pins: 14.1 every body in `BODIES` and every kit item has a cell; 14.2 at most
`THUMBS.perFrame` cells a frame; 14.3 no picture is rendered while the run
drives.

#### Slice 15 — the preview; GOALS in pictures (1 day)

R10's focus preview and its horn, and GOALS in pictures.

Files: `src/ui/wall/garage.ts`, `src/render/cars/PlayerCar.ts`,
`src/render/cars/kitMesh.ts`, `src/audio/` (the horn on focus); tests
`tests/ui/wall.test.ts`.

Pins: 15.1 a preview then a leave restores the car exactly; 15.2 a buy after
a preview keeps it; 15.3 GOALS' rows are the pictures' model.

### Phase G — the gate

#### Slice 16 — the gate

`npm run verify:gate`; `look` with `LOOK_GATE=1` at the three sizes, and the
board, before and after, put in front of Marcin; `screens` (the new selectors
and words at the ten sizes, TOTALS without scrolling at 800×450), `game`,
`heat`, `life`, `city`; `perf` twice against M8.8's gate build; `balance`
(the first hour's income without the rings' coins, quoted); the
twelve-year-old's pass (§17.7) on the new screens, the question "what is
this number?" added; STYLE rewritten (the UI and the world). Report
`docs/M8.9_REPORT.md`; 0.8.9. The milestone closes at Marcin's nine.

## 4. Verification

### 4.1 Headless tests

About 55 new pins across §3's files; no pin loosened. The pins that must
change, each with its reason in PROGRESS when it does: `corners` (the
gauge), `minimap` (the sizes and the kinds), `signs` (the size and the
fold), `voice` and `lanes` (the queue), `words` and `lang` (the new words),
`wall` and `wallGrid` (the layout), the jobs' pins that count the rings'
coins, `save` (its version).

### 4.2 e2e (at the gate)

`look.spec.ts` (new; the board and §1.2's targets); `screens.spec.ts` (its
`drivingShown` lists take the gauge's selectors, its texts the new words);
`game.spec.ts` (the wall's keys and the offer on the new layout); `heat`,
`life` and `city` unchanged (time to control with the fonts preloaded).

### 4.3 Performance protocol

Bases: M8.8's gate build, `npm run perf` twice. Expected costs: 60 KB of
fonts (control does not wait for them); a byte per city vertex; two draws in
the sky; the room's light at zero outside the door (the light count never
changes); the atlas at the door only (a few cells a frame, a 2D canvas of 8
MB at most); a lower sun costs nothing in the shadow map (the same size). A
frame p95 above the bases in both runs is a regression to fix before the
gate.

### 4.4 Budgets that must hold at the gate

| Check | Limit | Where |
|---|---|---|
| `npm run verify:gate` | green, lint 0 warnings | tools/verify.mjs |
| Startup bytes before gameplay-start | ≤ 8 MB target, 12 MB fail | tools/budget.mjs |
| The fonts | ≤ 80 KB | tests/ui/fonts.test.ts |
| Time to control, 20 Mbit + CPU ×4 | ≤ 6 s | e2e/city.spec.ts |
| Frame p95 under 4×, real GPU | < 33.4 ms, not above M8.8's | e2e/perf.spec.ts |
| Draws / tris, low tier | 150 / 250k (+2 draws at most) | e2e/city.spec.ts |
| JS heap | ≤ 250 MB | perf |
| Save, everything filled | < 32 kB | tests/sim/save.test.ts |
| Text at 800×450 | ≥ 13 px | e2e/look.spec.ts |

## 5. Records

PROGRESS per slice, with the stills' numbers; `docs/M8.9_REPORT.md` with the
board and §1.2's table filled; STYLE rewritten: the UI (the tokens, the two
styles, the scale, the corners and the gauge, the radar, the signs, the
showroom) and the world (the light, the sky, the palette, the glow);
`docs/ASSETS.md` (Rubik: source, licence, the subsets); ARCHITECTURE records
for the rem scale, the voice's queue, the glow attribute, the atlas and the
room's light; DESIGN §3.2 (the rings' coins), §17.2 (the corners), §17.4
(the glyphs drawn), §19 (the words), §20 (the radar's dots, the signs'
size); `CLAUDE.md`'s fixed decisions (the colour table, the typeface);
`docs/CRAZYGAMES.md` (legibility at the ten sizes); M9's cover brief (the
gate's frames); BACKLOG for whatever stays.

## 6. Gate criteria (definition of done for M8.9)

1. Slices 0–15 committed with their pins; each look slice's numbers in
   PROGRESS.
2. `verify:gate`, `look`, `screens`, `game`, `heat`, `life`, `city` green;
   the perf A/B and the balance's income quoted.
3. §1.2's measured targets met.
4. Every budget in §4.4 holds.
5. The twelve-year-old's pass: every number has its glyph or word, no colour
   means two things, no word he would ask about, one message at a time in
   every state.
6. **Marcin's mark: nine or ten**, on the board and in play. Below nine his
   notes become slices and the gate runs again.

## 7. API facts and traps

- **The scale is rem, not `zoom`**: CSS `zoom` multiplies viewport units
  inside the zoomed layer (a `100vh` wall grows past the screen) and moves
  every client rectangle. With rem, `getBoundingClientRect`, the full map's
  picking and the e2e overlap rule stay as they are. The media queries stay
  in px; the dev panel keeps px.
- **The pay label is placed in screen pixels** from the camera's projection:
  not in rem, and on the top layer, under the pause.
- **The canvases** (the radar, the full map, the gauge's arcs if drawn on
  one) size their backing store from their rect × DPR on every resize, and
  draw words only after `document.fonts.load('900 16px Rubik')`.
- **`font-display: swap`**: the first frames may use the fallback; the e2e
  waits for `document.fonts.ready` before it measures.
- **Rubik has no ★**: draw the star inline. Check its `tnum`; without
  tabular figures the speed and the money sit in fixed-width digit boxes.
- **An outline**: a ring of text-shadows works everywhere;
  `-webkit-text-stroke` eats into the glyph unless `paint-order: stroke
  fill` is honoured on HTML text. Check Chrome, Edge and Safari before
  choosing.
- **Never change the light count after boot**: three.js recompiles every lit
  program when lights are added, removed or hidden (a hitch at the door). The
  room's light exists from boot at intensity 0.
- **The showroom's framing** is `camera.setViewOffset`; clear it on the
  drive-out and set it again on a resize.
- **The turntable is drawn only**: `PlayerCar` adds a yaw while the door is
  shut; the sim's body never moves (the door and the drive-out read it).
- **The city's material chains `onBeforeCompile`**: `fadeShadowEdges` sets
  it, `roadPaint` wraps it; the glow wraps again and extends
  `customProgramCacheKey`. It goes after the lights and before
  `#include <fog_fragment>`, so the lit windows fade with distance.
- **The sky dome is 24×12 segments**: three stops fit its rings; the sun's
  disc is its own mesh.
- **`SUN_OFFSET` drives the light and the shadow's texel grid**
  (`shadows.ts`). A lower sun throws shadows past the map's 140 m, where
  `fadeShadowEdges` fades them; check acne at the grazing angle
  (`normalBias` 0.18).
- **The pictures' render target** takes `colorSpace = SRGBColorSpace` or the
  pictures come out dark; `readRenderTargetPixels` stalls the GPU, so a few
  cells a frame and only while the wall is up; flip the rows when copying.
- **The rings' coins** are `Coins.addExtra(…, 'rings')`, laid once by
  `jobs/place.ts`; removing them frees 256 slots and changes the jobs' coin
  counts.
- **The save's next version** after M8.8's carries the taught flags and the
  session count; the migration keeps every field.
- **`e2e/screens.spec.ts`** asserts `drivingShown`'s lists and English texts
  (TOTALS, CARS, BORROW); update them in the slice that changes each, and
  run the suite at the gate.
- **M8.8's bodies and kit need cells**; its role and BEST AT lines stay on
  the cards.
- **No per-frame DOM writes**: the outline makes a text's repaint dearer;
  the voice and the corners write on change only, as now.
- **`prefers-reduced-motion`**: the stars steady red, the turntable still,
  the goal's sign without its bob.

## 8. Five-minute playtest script (for the gate report)

1. `?fresh=1`, the intro's first second: one caption, and a sky that looks
   like a sunset?
2. Drive downtown and on the highway: warm light, long shadows, lit windows,
   the sun and the clouds. The line, the stars, the money and the dial read
   at a glance?
3. Look for a job: its sign, its word and its pay from 150 m; roll in:
   nothing blocks the view.
4. The first delivery: one card at a time, NEW only after DOSTARCZONO, the
   first bag teaching itself.
5. At three stars: the stars flash like a light bar; ZGUB ICH reads on the
   sky; the radar shows the route, the police and a garage.
6. Get boxed: the ticket alone (and POŻYCZ with it).
7. A door: the showroom and the car turning; CARS and STYLE with pictures;
   move over the toppers: the car wears each, the horns sound.
8. The full map: only what is out; the names clear.
9. The same at 800×450 and full screen at 1920×1080.
10. A mark out of ten for the look.

## 9. Knobs

`src/ui/colors.ts` (the tokens); `src/ui/scale.ts` (`HUD_SCALE`); the type's
sizes in `src/ui/styles.css`; `src/render/sky.ts` (`SKY`: the sun, the fill,
the stops) and `SUN_OFFSET` in `src/render/shadows.ts`; `CITY_COLORS` and
the accents in `src/sim/palette.ts`; `GLOW` in `src/render/city/glow.ts`
(the shares, the gain, the gradient); `MINIMAP` (the sizes);
`SIGN_MIN_PX`, `SIGN_FOLD`, `GOAL_SCALE` in `src/render/run/signs.ts`; the
voice's order, `STALE_NEWS` and the teaching lines in
`src/ui/hud/voice.ts`; `SHOWROOM` in `CameraDirector` (the angle, the turn,
the frame's share); `THUMBS` (the cell, the cells a frame).

## 10. Reviewer checklist (Claude, at the gate)

R1–R12 honoured, each with its pin or its measured number; §1.2's table
filled from the look suite; §1.4 stayed out; each trap of §7 handled; pins
changed only where §4.1 names them, each with its reason in PROGRESS; the
board shown to Marcin and his mark quoted.
