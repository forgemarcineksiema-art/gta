/** The airfield's and the islet's moving places drawn (M8.10 slice 12): none yet. */
import type * as THREE from 'three';
import type { Island } from '../../../sim/island/Island';
import type { PlaceView } from './index';

export function airfieldViews(_group: THREE.Group, _island: Island): PlaceView[] {
  return [];
}
