/** Coral Quay's moving places drawn (M8.10 slice 11): none yet (the ferris wheel and the duck come with it). */
import type * as THREE from 'three';
import type { Island } from '../../../sim/island/Island';
import type { PlaceView } from './index';

export function quayViews(_group: THREE.Group, _island: Island): PlaceView[] {
  return [];
}
