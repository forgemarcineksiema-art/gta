/** Sunset Works' moving places drawn (M8.10 slice 9): none yet (the freight train and the crossings' barriers come with it). */
import type * as THREE from 'three';
import type { Island } from '../../../sim/island/Island';
import type { PlaceView } from './index';

export function worksViews(_group: THREE.Group, _island: Island): PlaceView[] {
  return [];
}
