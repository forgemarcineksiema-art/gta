/**
 * The airfield's and the islet's places (M8.10 slice 12, docs/M8.10_PLAN.md): the runway, the hangars, the control
 * tower, the mega-ramp, the islet. None yet.
 */
import type { Place, PlaceContext } from './place';

export function airfieldPlaces(_ctx: PlaceContext): Place[] {
  return [];
}
