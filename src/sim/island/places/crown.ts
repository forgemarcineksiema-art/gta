/**
 * Crown Heights' places (M8.10 slice 8, docs/M8.10_PLAN.md): the summit's plaza and the Crown Tower, the serpentine,
 * the quarry, the multi-storey car park, the police headquarters, the arcade, the cliffs' rocks, the hideout. None yet.
 */
import type { Place, PlaceContext } from './place';

export function crownPlaces(_ctx: PlaceContext): Place[] {
  return [];
}
