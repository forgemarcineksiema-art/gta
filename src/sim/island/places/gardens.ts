/**
 * Palm Gardens' places (M8.10 slice 10, docs/M8.10_PLAN.md): the botanic garden and the Glasshouse, the golf course,
 * the dunes, the beach, the back gardens' shortcuts. None yet.
 */
import type { Place, PlaceContext } from './place';

export function gardensPlaces(_ctx: PlaceContext): Place[] {
  return [];
}
