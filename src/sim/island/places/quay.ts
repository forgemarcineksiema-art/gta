/**
 * Coral Quay's places (M8.10 slice 11, docs/M8.10_PLAN.md): the marina, the pleasure pier and its ferris wheel, the
 * reef, the lighthouse, the Coral Hotel, the stadium, the giant duck. None yet.
 */
import type { Place, PlaceContext } from './place';

export function quayPlaces(_ctx: PlaceContext): Place[] {
  return [];
}
