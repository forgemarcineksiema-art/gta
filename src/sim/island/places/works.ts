/**
 * Sunset Works' places (M8.10 slice 9, docs/M8.10_PLAN.md): the port, the dry canal, the railway and the freight
 * train, the Waterworks, the scrapyard. None yet.
 */
import type { Place, PlaceContext } from './place';

export function worksPlaces(_ctx: PlaceContext): Place[] {
  return [];
}
