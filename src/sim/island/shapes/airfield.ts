/** The airfield's and the islet's shapes in the ground (M8.10 slice 12): none yet (the islet's landing comes with its slice). */
export function airfieldShape(_x: number, _z: number, h: number): number {
  return h;
}
