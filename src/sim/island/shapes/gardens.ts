/** Palm Gardens' shapes in the ground (M8.10 slice 10): none yet (the golf's dunes and the Glasshouse's hill come with its slice). */
export function gardensShape(_x: number, _z: number, h: number): number {
  return h;
}
