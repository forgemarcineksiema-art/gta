/** Coral Quay's shapes in the ground (M8.10 slice 11): none yet (the reef's shallows and the stadium's bowl come with its slice). */
export function quayShape(_x: number, _z: number, h: number): number {
  return h;
}
