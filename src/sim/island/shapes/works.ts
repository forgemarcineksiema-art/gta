/** Sunset Works' shapes in the ground (M8.10 slice 9): none yet (the dry canal's channel comes with its slice). */
export function worksShape(_x: number, _z: number, h: number): number {
  return h;
}
